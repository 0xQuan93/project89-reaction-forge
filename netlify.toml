[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  # Use esbuild for TypeScript functions
  node_bundler = "esbuild"

# Remove or comment out the extensions block if it exists
# [[extensions]]
#   package = "netlify-plugin-..." 
